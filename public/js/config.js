// js/config.js

const CONFIG = {
    // Only the base URL of your API (ensure no trailing slash)
    API_BASE_URL: 'https://geo-froggy-backend.devhhtk.workers.dev',
};

// Make it globally available
window.APP_CONFIG = CONFIG;

console.log('App Config initialized:', CONFIG);
